export enum ExampleEnum {
  Zero = 0,
  One,
}

export enum SignInTabEnum {
  Account,
  PhoneNumber,
}

export enum AlternativeFunctionEnum {
  Program = 0,
  Notification,
  PressRelease,
  DelegateDetail,
}
